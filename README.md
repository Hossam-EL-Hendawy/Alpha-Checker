# Alpha-Checker
Alpha Checker is a Python-based tool that determines whether a link is safe or not. It analyzes URLs using checks like HTTPS, SSL certificates, DNS, domain age, and phishing patterns, then classifies them as safe, suspicious, or dangerous to help users avoid cybersecurity risks.
